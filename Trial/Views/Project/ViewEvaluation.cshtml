@model Trial.Models.FeedbackViewModel

<div class="modal-header">
    <button type="button" class="close" data-dismiss="modal">&times;</button>
    <h3>EVALUATION</h3>
</div>


<div class="modal-body">


    <div class="row">
       
        Team Leader :  <br /><b> @Model.User.UserName</b>
       
        
        <br /><br />

        Feedback : <br /> <b>@Model.Feedback.FeedbackDesc</b>
      <br /><br />
        Communication:


        @if (Model.Feedback.Communication == 1)
            {
            <p>
                <b>Poor</b>

            </p>
        }

        @if (Model.Feedback.Communication == 2)
            {
            <p>
                <b>Average</b>

            </p>
        }

        @if (Model.Feedback.Communication == 3)
            {
            <p>
                <b>Good</b>

            </p>
        }

        @if (Model.Feedback.Communication == 4)
            {
            <p>
                <b>Very Good</b>
            </p>
        }

        @if (Model.Feedback.Communication == 5)
            {
            <p>
                <b>Excellent</b>
            </p>
        }


        <!-------------------------------------------------------------------------------------->

        <br /> Team Work:
        @if (Model.Feedback.TeamWork == 1)
            {
            <p>
                <b>Poor</b>

            </p>
        }

        @if (Model.Feedback.TeamWork == 2)
            {
            <p>
                <b>Average</b>

            </p>
        }

        @if (Model.Feedback.TeamWork == 3)
            {
            <p>
                <b>Good</b>

            </p>
        }

        @if (Model.Feedback.TeamWork == 4)
            {
            <p>
                <b>Very Good</b>
            </p>
        }

        @if (Model.Feedback.TeamWork == 5)
            {
            <p>
                <b>Excellent</b>
            </p>
        }


        <!-------------------------------------------------------------------------------------->

        <br /> Problem Solving:
        @if (Model.Feedback.ProblemSolving == 1)
            {
            <p>
                <b>Poor</b>

            </p>
        }

        @if (Model.Feedback.ProblemSolving == 2)
            {
            <p>
                <b>Average</b>

            </p>
        }

        @if (Model.Feedback.ProblemSolving == 3)
            {
            <p>
                <b>Good</b>

            </p>
        }

        @if (Model.Feedback.ProblemSolving == 4)
            {
            <p>
                <b>Very Good</b>
            </p>
        }

        @if (Model.Feedback.ProblemSolving == 5)
            {
            <p>
                <b>Excellent</b>
            </p>
        }

        <!-------------------------------------------------------------------------------------->

        <br /> Work Knowledge:
        @if (Model.Feedback.WorkKnowledge == 1)
            {
            <p>
                <b>Poor</b>

            </p>
        }

        @if (Model.Feedback.WorkKnowledge == 2)
            {
            <p>
                <b>Average</b>

            </p>
        }

        @if (Model.Feedback.WorkKnowledge == 3)
            {
            <p>
                <b>Good</b>

            </p>
        }

        @if (Model.Feedback.WorkKnowledge == 4)
            {
            <p>
                <b>Very Good</b>
            </p>
        }

        @if (Model.Feedback.WorkKnowledge == 5)
            {
            <p>
                <b>Excellent</b>
            </p>
        }

        <!-------------------------------------------------------------------------------------->
        <br /> Independant Action:

        @if (Model.Feedback.IndependantAction == 1)
            {
            <p>
                <b>Poor</b>

            </p>
        }

        @if (Model.Feedback.IndependantAction == 2)
            {
            <p>
                <b>Average</b>

            </p>
        }

        @if (Model.Feedback.IndependantAction == 3)
            {
            <p>
                <b>Good</b>

            </p>
        }

        @if (Model.Feedback.IndependantAction == 4)
            {
            <p>
                <b>Very Good</b>
            </p>
        }

        @if (Model.Feedback.IndependantAction == 5)
            {
            <p>
                <b>Excellent</b>
            </p>
        }
    </div>
    <div style="text-align:left;" class="row modal-footer">
        <div class="col-md-4 col-md-offset-4">

            <a title="View Evaluation" id="approve-btn" class="modal-close-btn btn btn-primary" data-dismiss="modal">
                Close
            </a>
        </div>
    </div>
</div>
<script type="text/javascript">
    $(function () {
        $('#approve-btn').click(function () {
            $('#modal-container').modal('hide');
        });
    });
</script>

